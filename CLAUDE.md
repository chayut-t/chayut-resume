# chayut-resume

Read `DEVELOPMENT.md` before making changes — it covers the build pipeline, the
resume.md editing conventions, and the privacy rules (public repo: no phone, email,
or location may appear in any tracked file or output; `legacy/` stays gitignored).
